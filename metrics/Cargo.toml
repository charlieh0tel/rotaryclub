[package]
name = "rotaryclub-metrics"
version = "3.2.0"
edition = "2024"
publish = false
description = "Measurement instruments for the rotaryclub receiver: gates, sweeps, probes and censuses."

[dependencies]
rotaryclub = { path = "..", features = ["simulation"] }
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
serde_json = "1.0"
